export const BASE_URL = 'https://pokeapi.co/api/v2/';

// export const captureTypes = (listData, setTypes) => {
//     const allTypes = listData.types;
//     const arrayTypes = allTypes.map((elemento) => elemento.type.name);
//     setTypes(arrayTypes);
// };
